package com.example.dailysync.core.enums

enum class ErrorType {
    NETWORK_ERROR,
    WEAK_PASSWORD,
    EMAIL_OCCUPIED,
    ACCOUNT_NOT_FOUND,
    INVALID_CREDENTIALS,
    AUTH_ERROR,
    UNKNOWN_ERROR,
    EMPTY_THOUGHT
}
